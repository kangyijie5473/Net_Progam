PROJECT(Logger)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

set( CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}  -pthread" )

AUX_SOURCE_DIRECTORY(. DIR_SRCS)
ADD_EXECUTABLE(Logger ${DIR_SRCS})
#TARGET_LINK_LIBRARIES(LOgger Test)
